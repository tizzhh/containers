#include "array.hpp"
#include <array>

int main() {
  // s21::Array<int, 4> a;
  // a.fill(4);
  // for(auto b : a) std::cout << a[3];
  // std::array<int, 4> b;
  
  // int* b = new int[a.capacity()];
  
  // b <- a;
  // delete[] a;
  // int *a = new int[b.size() * 2]

  // realloc
  int n = prev_vector.size();
  int a[n];
  prev_vector --> a
  delete [] a;
  int* a = new int[n * 2]

}